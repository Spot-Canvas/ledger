# Ledger Service Configuration
# Copy to .env and adjust values for local development

# HTTP server port
HTTP_PORT=8080

# Database connection
# For local development with docker-compose:
DATABASE_URL=postgres://spot:spot@localhost:5432/spot_canvas?sslmode=disable

# For Cloud SQL (GCP deployment) — set these instead of DATABASE_URL:
# CLOUDSQL_INSTANCE=project:region:instance
# DB_NAME=spot_canvas
# DB_USER=spot
# DB_PASSWORD=your-password

# NATS connection
# For local development with docker-compose:
NATS_URLS=nats://localhost:4222

# For Synadia Cloud / NGS:
# NATS_CREDS_FILE=/path/to/credentials.creds
# Or provide raw credentials content (for Cloud Run secrets):
# NATS_CREDS=<raw-creds-content>

# Logging
LOG_LEVEL=info
# Options: trace, debug, info, warn, error, fatal, panic

# Environment
ENVIRONMENT=development
# Options: development, staging, production
