## Requirements

_All NATS-based trade ingestion requirements have been removed. Trade ingestion is now handled exclusively by the platform API (`POST /api/v1/trades`). The traderd binary no longer connects to NATS for ingestion purposes._

_See `cli-platform-direct` spec for how the CLI submits trades to the platform API._
